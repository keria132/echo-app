export const userEndpoints = {
  base: '/user/',
  search: '/user/search',
};

export const messagesEndpoints = {
  base: '/messages/',
  send: '/messages/send/',
};

export const chatEndpoints = {
  base: '/chats/',
};
